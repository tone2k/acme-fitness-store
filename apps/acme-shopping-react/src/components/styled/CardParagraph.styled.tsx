import {styled, Typography} from "@mui/material";

export const CardParagraphStyled = styled(Typography)(({ theme }) => ({
    fontFamily: 'Roboto, sans-serif',
    fontSize: '1em',
    fontWeight: 400,
    color: '#140A00',
    lineHeight: '140%'
}));
